[package]
name = "iou-fm"
version = "0.2.37"
description = "iou.fm – IBAN · Order · Überweisung"
authors = ["fork and merge UG"]
edition = "2021"

# Bibliotheks-Crate (von main.rs aufgerufen; Name für mobile/Desktop einheitlich).
[lib]
name = "iou_fm_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-http = "2"
tauri-plugin-process = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Auto-Update + biometrisches Entsperren nur auf Desktop.
[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-updater = "2"
robius-authentication = "0.1"       # Biometrie-Prompt (Touch ID / Windows Hello)

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
